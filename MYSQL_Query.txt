#TO DO LIST 
- 컬럼 사이즈 체크
- creaiontime timestamp추가 

# 사용자 계정용 DB CREATE 
CREATE DATABASE user default CHARACTER SET UTF8 collate utf8_general_ci;

# MASTER(읽기전용) 계정정보 TABLE CREATE 
CREATE TABLE info_m (
    id int(11) NOT NULL AUTO_INCREMENT,
    name varchar(50) NOT NULL,
    nickname varchar(100) NOT NULL,
    passwrod varchar(100) NOT NULL,
    tel varchar(50) NOT NULL,
    email varchar(255) NOT NULL,
    gender varchar(10),
    PRIMARY KEY (id)
);

# MASTER(쓰기전용) 계정정보 TABLE CREATE 
CREATE TABLE info_s (
    id int(11) NOT NULL AUTO_INCREMENT,
    name varchar(50) NOT NULL,
    nickname varchar(100) NOT NULL,
    passwrod varchar(100) NOT NULL,
    tel varchar(50) NOT NULL,
    email varchar(255) NOT NULL,
    gender varchar(10),
    PRIMARY KEY (id)
);